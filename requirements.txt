To run the credit card fraud detection pipeline in a local or production environment, create a requirements.txt file with the following dependencies. These versions are consistent with standard data science environments as of late 2025: 
text
# Data Manipulation and Analysis
pandas>=2.2.0
numpy>=1.26.0

# Data Visualization
matplotlib>=3.8.0
seaborn>=0.13.0
plotly>=5.20.0
# Machine Learning and Modeling
scikit-learn>=1.4.0
xgboost>=2.0.0

# Handling Imbalanced Data
imbalanced-learn>=0.12.0
Use code with caution.
Installation Instructions
You can install all required packages at once using the pip package manager:
Save the text above into a file named requirements.txt in your project directory.
Run the following command in your terminal:
bash
pip install -r requirements.txt
Use code with caution.
Compatibility Note
Python Version: These libraries typically require Python 3.10 or higher as of 2025.
SMOTE: The imbalanced-learn library provides the SMOTE implementation used in your code. Ensure it is installed separately as it is not part of the standard scikit-learn package. 
